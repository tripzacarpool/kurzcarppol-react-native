// BookingStatusPassenger removed — component deleted per user request.
// If other parts of the app still import this component, it now returns null (safe no-op).
import React from 'react';

export default function BookingStatusPassenger(): null {
  return null;
}
